#include<stdio.h>
int main()
{
    int n1,n2;
    printf("enter the number ");
    scanf("%d", &n1);
    int sum=n1;
    printf("first power is %d ",n1=n1);
    printf("\nsecond power is %d",n1=n1*n1);
    if(n2=sum)
    {
    printf("\n third power is %d",sum=n2*n2*n2);
    }
    return 0;
}