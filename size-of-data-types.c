#include<stdio.h>
int main()
{
    int a;
    char b;
    float c;
    double d;
    printf("size of int is: %ld", sizeof(a));
    printf("\nsize of char is: %ld",sizeof(b));
    printf("\nsize of float is: %ld",sizeof(c));
    printf("\nsize of double is: %ld",sizeof(d));
    return 0;
}